PROJECT PROGRESS LOG
====================

Project: [Project Name]
Started: [Date]

SESSION 0 - Initializer Agent
-----------------------------
Date: [Date]
Worked on:
- Set up project structure
- Created feature_list.json with [count] features
- Created init.sh for development server and testing
- Initialized git repository
- Made initial commit

Feature Status:
- Total features: [count]
- Passing: 0
- Failing: [count]

Next Steps:
- Coding agents should start working on the highest-priority failing features
- Run init.sh before implementing new features
- Test thoroughly before marking features as passing

---

### SESSION <N> — <ISO date>

**Worked on:** <feature_id> @ <commit_sha> — [Feature description]

**Status:** [one rendered label from `## Status values` below]

**Features now passing:** [feature_id, ...] (<X> / <Y> total)

**Notes:** [free text, max 500 chars; "(none)" if nothing]

---

## Status values

Authoritative definitions: `tools/_constants.py` (`SESSION_STATUSES` /
`SESSION_STATUS_LABELS`). The values below are rendered labels only:

- (`implemented_and_tested`) → "Implemented and tested"
- (`implemented_not_tested`) → "Implemented, not yet tested"
- (`in_progress_blocked`) → "In progress, blocked"
- (`deferred`) → "Deferred"
- (`no_op`) → "No-op"

### SESSION 1 — 2026-09-12

**Worked on:** feat-003 (monorepo skeleton); feat-004 (LICENSE+SPDX); feat-005 (logging); feat-008 (feature_list_io library extraction, T-014)

**Status:** Implemented and tested

**Features now passing:** feat-003, feat-004, feat-005, feat-008 (4 / 56 total)

**Notes:** feat-008: Ported HARNESS feature_list logic to packages/common/heddle_common/feature_list_io.py (and atomic_io.py). HARNESS/tools/_feature_io.py + _feature_state.py are now thin re-exports. feature_list.py CLI dispatches through the shims with no source change. Verified: feature_list.py status byte-identical before/after; handoff_check.py output byte-identical (also fixed an unrelated Windows bug where it used 'python' instead of sys.executable); load+save round-trip byte-identical. 12 new unittest tests pass for feature_list_io. Total: 25 (logging) + 12 (feature_list_io) = 37 tests across Python+TS.

---

### SESSION 2 — 2026-09-12

**Worked on:** feat-009 @ 945cc90 — implement schema_version field on feature_list.json (T-022); follow-up @ 8c0d4c6 — fill in CODE_STYLE.md Part 2

**Status:** Implemented and tested

**Features now passing:** feat-009 (5 / 56 total)

**Notes:** feat-009: Added SCHEMA_VERSION=1, SCHEMA_VERSION_MAX=1, and SchemaVersionError to heddle_common/feature_list_io.py. load() defaults missing schema_version to 0; refuses versions above SCHEMA_VERSION_MAX with a clear "please upgrade heddle" message; rejects non-int / negative values. save() always stamps SCHEMA_VERSION so legacy files migrate in place. Migrated both feature_list.json (root) and HARNESS/feature_list.json to schema_version=1. Re-exported SCHEMA_VERSION / SCHEMA_VERSION_MAX / SchemaVersionError from package init. HARNESS/tools/feature_list.py works unchanged (auto-inherits via shim). Updated TestRoundTrip; added TestSchemaVersion class (7 new tests). 32/32 unittest pass. feat-010, feat-016 now unblocked. Follow-up: filled in 27 [fill-in] placeholders in CODE_STYLE.md Part 2 (handoff_check code_style_fillins was the only pre-existing failure). Part 1 untouched. handoff_check now 10/10 PASS.

---

### SESSION 3 — 2026-09-12

**Worked on:** feat-007 @ 9ddf847 — HEDDLE_FAKE_LLM fixture format + loader + FakeLLM + chokepoint (T-018, T-031)

**Status:** Implemented and tested

**Features now passing:** feat-007 (6 / 56 total)

**Notes:** feat-007: Added heddle_common/fake_llm.py with ScriptedToolCall / ScriptedResponse dataclasses, load_fixture() (strict schema validation: rejects unknown keys, bad stop_reason, non-string tool names, etc.; enforces MAX_FIXTURE_BYTES + MAX_FIXTURE_RESPONSES), FakeLLM (sequential or loop=True; FakeLLMExhausted when non-looping runs out), fake_llm_or_real() chokepoint that returns FakeLLM only under HEDDLE_FAKE_LLM ∈ {1,true,yes,on} AND a fixture_path is provided (loud failure otherwise — no silent fallback). Sample fixture shipped at packages/daemon/tests/fixtures/sample_agent_run.json (4 turns: Glob→Read→Edit→end_turn). feat-019's agent_runtime.py will call fake_llm_or_real(real_factory=..., fixture_path=...) with no branching of its own. 30 new fake_llm tests + 32 existing = 62/62 unittest pass. handoff_check 10/10. feat-006 (CI), feat-052 (heddle test) now unblocked.

---

### SESSION 4 — 2026-09-12

**Worked on:** feat-010 @ d146902 — extended feature fields: kind/fixes/enhances/superseded_by/implementation_model (D-018, D-023, D-054, D-055, D-060)

**Status:** Implemented and tested

**Features now passing:** feat-010 (7 / 56 total)

**Notes:** feat-010: Added KINDS=(feature,bugfix,enhancement) + DEFAULT_KIND=feature to feature_list_io. _ensure_extras now backfills all five new fields; load() runs _ensure_extras over every feature so legacy files gain the schema on read. add() gained 5 kwargs (kind/fixes/enhances/superseded_by/implementation_model) with full validation: kind=bugfix requires fixes+target exists+target≠self; kind=enhancement same for enhances; superseded_by is comma-string or list, all ids must exist; empty superseded_by normalizes to null. next_feature() now skips features whose superseded_by != null (D-054). save() always writes all five fields (null when unset). 19 new tests cover: defaults backfill, validation errors, round-trip, old-format migration, save+load byte-identical, next_feature skip of superseded. 81/81 unittest pass. handoff_check 10/10. feat-016, feat-045, feat-046, feat-054 now unblocked.

---

### SESSION 5 — 2026-09-12

**Worked on:** feat-011 — ~/.heddle/configs.yaml + heddle configs list (T-023, D-055, T-015)

**Status:** Implemented and tested

**Features now passing:** feat-011 (8 / 56 total)

**Notes:** feat-011: Added heddle_common/configs_io.py (Config dataclass, load/save/add/update/delete/get/list, ensure_configs first-run UX, resolve_api_key + resolve_api_key_for_config chokepoints per T-015). DEFAULT_TEMPLATES ships the four v0.1 templates: anthropic-claude-sonnet / openai-gpt-4 / bedrock-claude / ollama-llama. Added PyYAML>=6.0 to heddle-common deps. CLI: `heddle configs list [--config-path]` rendered via Click; marks ✓/✗ for api_key_env resolution; NEVER prints the resolved value (T-015). 33 new configs_io tests (defaults, schema, first-run idempotency, mutations, T-015 chokepoint). End-to-end CLI smoke: `heddle configs list --config-path /tmp/x.yaml` exits 0, creates file with 4 defaults, renders them with env-var set/unset markers. 114/114 unittest pass. handoff_check 10/10. feat-023 (4 LLM providers), feat-031 (config resolution) now unblocked.

---

SUMMARY
-------
Total features: 54
Passing: 17
Failing: 37
In progress: 1
Blocked: 0
Deferred: 0
Last updated: 2026-09-12

### SESSION 6 — 2026-09-12

**Worked on:** feat-016 @ f2b120e — backward-compat tests for feature_list_io; feat-012 @ fbd6b54 — ~/.heddle/projects.json registry; feat-013 @ a98f8a4 — ~/.heddle/.env fallback loader; feat-006 @ 576550b — GitHub Actions CI workflows; feat-017 @ 7894095 — Python daemon skeleton (asyncio + websockets)

**Status:** Implemented and tested

**Features now passing:** feat-016, feat-012, feat-013, feat-006, feat-017 (13 / 56 total)

**Notes:** 5 features across 5 commits. feat-016: tests/fixtures/old-harness-format.json + TestBackwardCompat (5 tests). feat-012: heddle_common/projects_io.py (Project dataclass, schema_version gate, add_project validates path exists, remove_project cascade deferred to feat-014) + 21 tests. feat-013: heddle_common/env_loader.py (parse_env_lines, load_env_file, ensure_env_loader writes .gitignore ONLY; EnvPermissionError on group/world-readable POSIX files, skipped on Windows ACLs) + 23 tests. feat-006: ci.yml (PR+push-to-main, matrix ubuntu/macos/windows, HEDDLE_FAKE_LLM=1, lockfile-keyed pnpm + per-package pip cache) + nightly.yml (cron 02:17 UTC, LIVE_LLM_TESTS-gated) + 13 ci-workflow tests. feat-017: packages/daemon/heddle_daemon/server.py (DaemonConfig loopback-only with clear error, JsonEnvelope per T-010, Daemon class with two-phase start+serve_forever, signal handlers, port-conflict OSError, websockets>=12.0) + __main__.py + 30 daemon tests (handshake, port-conflict, handler errors, version gate). 226/226 cross-package tests pass (163 heddle_common + 30 daemon + 13 ci-workflow + 20 prior passing feat; 3 POSIX-only env perm tests skipped on Windows). handoff_check 10/10. feat-014, feat-018, feat-023, feat-027, feat-028, feat-030, feat-052 unblocked.

---

### SESSION 7 — 2026-09-12


**Worked on:** feat-018 @ 723733a

**Status:** Implemented and tested

**Features now passing:** feat-018 (14 / 54 total)

**Notes:** feat-018: ProjectCheckpointStore + daemon project_path wiring. 36 new daemon tests (66 total). feat-014/019/024/047 unblocked; next: feat-019 self-written agent runtime.

---
### SESSION 8 — 2026-09-12


**Worked on:** feat-019 @ 4a2a8de

**Status:** Implemented and tested

**Features now passing:** feat-019, feat-020, feat-021 (17 / 54 total)

**Notes:** SESSION 8 shipped 3 features: feat-020 six self-written tools (45 tests); feat-021 sandbox middleware (39 tests); feat-019 agent runtime (20 tests). 17/54 passing. feat-022/044 unblocked next.

---
